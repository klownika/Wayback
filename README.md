
  # Wayback

  This is a code bundle for Wayback. The original project is available at https://www.figma.com/design/4zfafGCwM16G1ZyHvcSlJz/Wayback.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  